# Copy to .env.local (never commit real keys).

# Contact form — create a free form at https://formspree.io and paste its ID
# (the part after /f/ in the endpoint URL). If left empty, the form falls back
# to opening the visitor's email client with the message pre-filled.
VITE_FORMSPREE_ID=
